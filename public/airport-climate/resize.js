// Measure content, never the iframe viewport (which the parent resizes).
(()=>{let queued=false,last=0;function report(){if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;const main=document.querySelector('main');if(!main||parent===window)return;const height=Math.ceil(main.getBoundingClientRect().bottom+scrollY);if(height===last||!Number.isFinite(height)||height<120||height>20000)return;last=height;parent.postMessage({type:'airport-climate-height',height},location.origin)})}window.airportReportHeight=report;addEventListener('load',report);addEventListener('resize',report,{passive:true});new ResizeObserver(report).observe(document.querySelector('main'));document.fonts.ready.then(report);report()})();
